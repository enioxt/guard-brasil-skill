# artefato gerado (2.6MB) — regenerável via build.ts
dist/
# libs vendorizadas (baixadas pelo build/fetch) — não poluir git
vendor/*.min.js

# probe local do GLiNER (node_modules + modelo 120MB)
gliner-probe/
